import React from "react";
import logo from '../../../Assets/images/logo.png';
import './Logo.scss';

const Logo = (props) => {
    return(
        <img src={logo} alt={'logo'} />
    )
}

export default Logo;